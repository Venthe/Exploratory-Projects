import React from "react";

export const Root = (props: any) => (
    <React.StrictMode>
        ufrontend-example-a
      <pre>{JSON.stringify(props, undefined, 2)}</pre>
    </React.StrictMode>
    )